# Dataset Studio 现场一条龙手册

Dataset Studio 是独立的离线数据工作台，只负责相机采集、局域网标注、YOLO 数据检查和训练。它不连接、不启用、也不控制机械臂。

## 一、赛前准备

1. 把一个可离线训练的 Ultralytics 基础模型放到 `models/`，例如现有的 `models/yolov8s.pt`。不要依赖现场自动下载模型。
2. 双击 `run_dataset_studio.bat` 使用 RealSense；普通 USB 相机使用 `run_dataset_studio_usb.bat`。
3. Windows 首次询问防火墙时，只允许“专用网络”。
4. 主机浏览器打开 `http://127.0.0.1:8765`。页面“局域网地址”会列出队友可访问的地址。
5. 队友与主机连接同一局域网，在浏览器输入 `http://主机IP:8765`。无需安装标注软件。

若 `8765` 被占用，可在命令行改端口：

```bat
python -m dataset_studio.server --host 0.0.0.0 --port 8766 --camera realsense
```

## 二、从拍摄到标注

1. 在“项目”填写项目名和类别顺序，例如 `red_part, blue_part, defect`，保存后再采集。
2. 点击“刷新预览”确认画面，再用“拍摄并加入”连续采集；已有图片可批量导入。
3. 每位队友填写不同的标注员名称，点击“领取下一张”。一张图片同一时间只会分给一人，锁在十分钟无操作后自动释放。
4. 在画面拖动绘框；点击框可移动，拖右下角手柄可缩放。选择类别后保存。
5. 没有任何目标的图片必须勾选“无目标负样本”，不能误当成漏标。
6. “保存并下一张”会写入版本化 JSON 和标准 YOLO `.txt`，同时释放当前图片给其他队友复核。

已有标注后不能减少类别。允许在原位置改名或在末尾增加类别，但类别编号顺序必须始终与训练和比赛配置一致。

## 三、现场采集建议

- 每个类别至少采集 30 张，时间允许时以 80 到 200 张为目标。
- 覆盖远近、左右、旋转、遮挡、明暗、反光、不同背景和机械臂阴影。
- 留出约 10% 到 20% 的无目标、相似干扰物和空工作台负样本。
- 框紧贴目标可见边缘，不把大面积背景框进去；同一规则由全队统一执行。
- 严重模糊、过曝、目标小到不可辨认的图片直接删除，不要用错误标注补救。
- 训练前查看“数据就绪度”，保证每个类别都有框，且验证集至少有一张图。

## 四、训练

切换到“训练与模型”，选择本地基础模型后设置参数：

| 条件 | epochs | imgsz | batch | patience | device |
|---|---:|---:|---:|---:|---|
| 现场快速验证 | 30-50 | 640 | 8 | 10 | GPU 0 |
| 正式现场训练 | 80-150 | 640 | 8 | 20-30 | GPU 0 |
| 小目标较多 | 100-180 | 960 | 4 | 30 | GPU 0 |
| 无可用 CUDA | 20-50 | 640 | 2-4 | 10 | CPU |

点击“开始训练”后，程序会先冻结一份只含已完成标注的训练/验证集，生成标准 `data.yaml`，再启动独立 Ultralytics 进程。关闭网页不会中断训练；关闭 Dataset Studio 终端会结束服务，训练进程仍可能继续，因此不要在训练时关闭窗口。

每次训练输出到：

```text
models/field_models/<项目_时间>/
  best.pt
  last.pt                 可选
  model_profile.yaml
  metadata.json
```

训练快照、日志和状态保存在 `runs/dataset_studio/<项目>/`。每次训练使用独立目录，不会覆盖上一版模型。

## 五、比赛 GUI 使用模型

1. 训练完成后启动或切回 `run_competition_gui.bat`。
2. 点击“模型管理”，列表会自动扫描 `models/field_models/**/model_profile.yaml`。
3. 核对模型名、类别顺序、图片数和指标，选择后点击“应用所选模型”。
4. GUI 会先尝试加载新模型；成功后才更新 `configs/field.yaml` 并替换检测器。失败时旧模型和旧配置保持不变。
5. 正在运行模块流程时禁止切换模型。切换后先做单帧识别，不要直接启动机械臂动作。

已有外部 `.pt` 可在“模型管理”中点击“导入外部 .pt”。必须手工填写训练时的类别顺序和置信度，程序会复制权重并补齐 Profile。类别顺序不确定时不要导入用于自动分拣。

## 六、现场最短流程

1. 确认赛题需要本程序 YOLO，而不是只使用 DVS 的识别结果。
2. 启动 Dataset Studio，创建类别。
3. 一人采集，两到三人通过局域网并行标注。
4. 清理漏标和错误框，保证各类别都有样本。
5. 先用 30 到 50 epochs 快训，检查能否正确识别。
6. 时间允许再扩充数据并正式训练。
7. 在比赛 GUI 的“模型管理”应用模型。
8. 单帧识别核对类别，再做坐标预览，最后低速单件动作。

Dataset Studio 与 DobotVisionStudio 是两套不同组件：前者训练本程序使用的 YOLO，后者按现场任务通过串口或 TCP 返回视觉流程结果。是否同时使用两者由正式任务书决定。
