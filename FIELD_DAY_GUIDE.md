# 国赛现场联调清单

## 1. 启动前

1. 确认机械臂工作区无人、急停可用、吸盘和气路正常。
2. 机械臂 Dashboard 保持 `192.168.5.1:29999`，不要把 DVS 端口填到机器人配置中。
3. 启动 `run_competition_gui.bat`，确认底部仍显示 `192.168.5.1:29999`。
4. 点击“通信/高级”，先保存 DVS 通信参数，机器人动作保持关闭。
5. 点击“赛前自检”，离线检查必须无“阻止”项。

## 2. 确定 DVS 通信角色

| DVS 中的设备 | 本程序模式 | 本程序地址填写 |
|---|---|---|
| 串口设备 | `serial` | 填程序侧 COM 口，波特率等参数与 DVS 一致；虚拟串口通信时两端应选成对的不同 COM 口 |
| TCP 服务端 | `tcp_client` | DVS 所在计算机 IP 和 DVS 监听端口 |
| TCP 客户端 | `tcp_server` | 本机监听地址和端口，DVS 连接本机 |

同一台计算机通信时，只有在 DVS 明确配置为本机 TCP 服务端后才使用 `127.0.0.1`。教学案例的 `4000` 端口不是国赛固定端口。

## 3. 只验证通信

1. 在 GUI 中运行“DVS 通信端点扫描”。
2. 运行“DVS 触发与结果读取”，核对日志中的原始返回。
3. GUI 不便启动时运行：

```bash
python tools/probe_dvs.py --discover
python tools/probe_dvs.py --connect
python tools/probe_dvs.py --exchange --message get_photo
```

4. DVS 主动上报时取消“本程序先发送触发/查询字符串”，不要填写触发串。
5. 返回 JSON 时字段顺序留空；逗号报文填写 `,`；任意空格报文填写 `whitespace`。
6. 任务书要求向 DVS 下发任务号或参数时，在“DVS 全局变量”填写 `task=1; threshold=0.8`，并在触发模块前插入“设置 DVS 全局变量”。

赛前没有 DVS 设备时，可用 `run_mock_dvs.bat` 和 `configs/mock_dvs.yaml` 演练真实 TCP 收发；测试端口 `19001` 不得直接当作现场端口。

## 4. 配置任务字段

点击主界面的“任务向导”，选择最接近的题型：

| 题型 | 向导选择 | 连续任务处理方式 |
|---|---|---|
| 缺陷 OK/NG 分拣 | 缺陷或状态 OK/NG 分拣 | 状态判定 + 路由 |
| 颜色/类别分拣 | 颜色/类别/型号分拣 | 只标准化 + 路由 |
| 尺寸测量 | 尺寸测量或尺寸判定后分拣 | 尺寸公差判定 |
| OCR/条码/二维码 | OCR/条码/二维码识别 | 只标准化 + 识别字段校验 |
| 指定编码/白名单/格式后分拣 | OCR/条码/二维码核验后分拣 | 精确值/允许列表/正则 + 路由 |
| 缺陷 + 尺寸 + 编码等组合 | 综合自定义任务 | 状态/尺寸/识别/路由按需叠加 |
| 工件到孔位装配 | 视觉定位引导抓取/装配 | 只标准化 + 机器人动作 |
| 固定送检后分流 | 固定取料送检后按结果分流 | 固定动作 + 状态 + 路由 + 抓放 |

向导会创建新的 Profile 和永久现场配方，不需要打开 YAML。DVS 返回字段顺序必须与报文字段一一对应；JSON 报文留空。

## 5. 无运动验证

依次执行：

1. `DVS 触发与结果读取`
2. `标准化 DVS 返回字段`
3. 对应的判定、识别或路由模块
4. `预览并校验 DVS 动作坐标`

题目包含固定取料或送检步骤时，还要在“动作编辑器”填写示教位姿，并先运行“预览并校验固定动作序列”。

确认 GUI 中的判定、路线、抓取 XYZ 和放置 XYZ 正确。任何 Z 坐标低于 `robot.min_grasp_z_mm` 都必须先修正配置或标定，不能降低安全下限来绕过错误。

## 6. 首次动作

1. 连续任务保持关闭机器人动作，先重复读取 3 次。
2. 把机器人速度和下降速度设为低速，清空工作区。
3. 只执行一件的“按 DVS 坐标抓放/装配”。
4. 检查抓取偏差、释放位置、吸盘状态和回安全点动作。
5. 坐标正确后再启用连续任务的机器人动作；错误策略必须为“立即停止”。

## 7. 连续运行

1. 设置最多件数和空料状态字段，例如 `status=NO_TARGET`。
2. 运行“DVS 多工件连续任务”。
3. 日志应持续显示 `completed/最大件数`、判定和路线。
4. 急停会设置取消标志、停止运动并终止通信循环；复位后必须重新做一次无运动读取。
5. 完成后点击“导出结果”，保存逐件记录和分类统计。

## 常见错误

| 提示 | 排查方向 |
|---|---|
| TCP 连接超时 | DVS 客户端/服务端角色是否配反、防火墙、IP和端口 |
| 未返回数据 | DVS 流程是否执行发送模块、结束符是否一致、触发串是否匹配 |
| 返回字段数不匹配 | `response_fields` 数量、分隔符、DVS 格式化输出 |
| 缺少必需字段 | 字段名和 Profile `aliases` 是否对应 |
| 没有对应动作路线 | `route.map` 是否覆盖实际 OK/NG、颜色或类别 |
| 未配置路线放置坐标 | `place.route_poses` 是否包含路线对应的 6 轴位姿 |
| 动作坐标低于安全下限 | DVS 坐标系、Z 字段、单位、标定和偏移 |
