# 2026 国赛考题预测与模块覆盖矩阵

> 本文是正式题面发布前的历史预测。2026-08-18 正式题面已整理到 `../gs/2026-08-18_今日国赛题整理.md`；针对 2026-08-19 的更新预测见 `TOMORROW_TASK_PREDICTION.md`。

> 本文不是官方任务书。概率来自现有省赛任务、DobotVisionStudio 4.1.2 能力，以及教学资源中的综合任务和案例程序。

## 预测结论

| 预测题型 | 概率 | 资料证据 | 当前可拼装模块 | 现场主要配置 |
|---|---:|---|---|---|
| 颜色/类别识别后分拣 | 很高 | 实验15、项目10任务4；案例同时出现颜色抽取、BLOB、快速匹配、标定转换和机器人动作 | `vision_result.route` + `robot.result_pick_place` | 类别字段、路由表、料框位姿 |
| 缺陷检测后 OK/NG 分流 | 很高 | 实验13、项目10任务2；包含直线/圆弧/字符缺陷、条件检测和分支动作 | `vision_result.quality` + `vision_result.route` + `robot.result_pick_place` | 状态字段、OK/NG 值、两类放置点 |
| 视觉定位引导装配 | 高 | 项目10任务5要求将4种尺寸圆形工件装入对应孔位；另有手机定位装配案例 | `vision_result.normalize` + `robot.result_pick_place` | 工件坐标字段、孔位坐标字段、Z和姿态 |
| 综合尺寸测量/公差判定 | 高 | 实验14、项目10任务1明确要求边长、孔径、圆心距、圆边距；案例使用点点、圆圆、线圆测量 | `vision_result.measure` | 测量字段和每项 min/max |
| OCR + 条码 + 二维码综合识别 | 中高 | 实验12、项目10任务3要求包装任意摆放仍同时识别三类信息 | `vision_result.identify` | `identifier_fields` 和字段别名 |
| TCP/串口触发视觉流程并取结果 | 很高 | 四个综合实验均使用通信触发；DVS 4.1.2 支持串口、TCP客户端/服务端和主动上报 | `vision_studio.*` + `vision_result.normalize` | 模式、端点、触发串、结束符、字段表 |
| 多工件连续循环与组合任务 | 高 | 省赛已有连续分拣，综合案例普遍是“搬运到检测区-检测-再分流/装配” | 配方队列 + 上述业务模块 | 配方顺序、循环次数或现场动作要求 |
| 固定取料、送检、检测后分流 | 高 | 实验12–14均采用“取料区→视觉检测区→结果区域”的复合动作 | `robot.sequence_plan` + `robot.sequence_execute` + `task.dvs_cycle` | 现场示教位姿、循环前后动作 |
| 多槽位摆放或堆叠 | 中高 | 教学资源包含分拣、堆叠和装配类案例，国赛容易在数量/顺序上增加变化 | `place.route_slots` / `place.route_stacks` | 槽位位姿、层高、容量 |
| 下发任务号或视觉参数 | 中 | DVS 4.1.2 手册明确提供 `SetGlobalValue:name=value` | `vision_studio.set_globals` | 全局变量名和值 |

## 关键证据

- 教学 Python 例程使用 `127.0.0.1:4000` 和触发串 `get_photo`，但返回格式随任务变化：`NG x y`、`x,y`、`颜色,x,y`。因此程序不能写死单一协议。
- 包装识别 `.sol` 包含快速匹配、位置修正、OCR、条码、二维码、格式化、接收和发送数据。
- 缺陷检测 `.sol` 包含直线边缘缺陷、圆弧边缘缺陷、字符缺陷、条件检测、标定转换和分支。
- 尺寸测量 `.sol` 包含边缘交点、点点、圆圆、线圆测量和单位转换。
- 装配 `.sol` 同时包含工件定位、目标孔位定位、标定转换和多段机器人动作。

## 现场组装原则

1. 先确定 DVS 是串口、TCP 客户端还是 TCP 服务端，填写 `vision_studio`。
2. 在 DVS 格式化模块中固定输出顺序，并在 `response_fields` 写同样的字段顺序。
3. 在 GUI 点击“任务向导”，选择题型并填写字段别名、阈值、规则和位姿；无需编辑 YAML。
4. 向导会自动创建活动 Profile、永久配方和推荐模块队列。
5. 第一次先运行“DVS 触发/取结果”和“预览并校验 DVS 动作坐标”，核对结果后再运行带机械臂动作的模块。
6. 模块队列确认后在 GUI 保存为现场配方，避免重启后重新编排。

机械臂 Dashboard 地址继续使用省赛值 `192.168.5.1:29999`。DVS 的 TCP/串口参数是独立通信链路，未知时必须保持空值，不能猜测。
