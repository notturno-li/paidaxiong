# 国赛现场零 AI 操作手册

本程序的现场目标是：不写 Python、不改 YAML、不联网、不使用 AI。拿到任务书后只做选择、填表、示教和预检。

## 一、不可修改项

- 机械臂 Dashboard：`192.168.5.1:29999`。
- DVS 的 IP、TCP 端口、COM 口不得猜测，必须以现场设备或任务书为准。
- 所有机器人动作必须先通过“赛前自检”和对应的无运动预览。
- 包含机器人动作的连续任务遇错必须立即停止。
- 不得通过降低 `min_grasp_z_mm` 绕过低 Z 报错。

## 二、拿到任务书后的最短流程

1. 若题目需要现场训练本程序 YOLO，先按 `DATASET_STUDIO_GUIDE.md` 完成拍摄、多人标注和训练；只使用 DVS 返回结果时跳过此步。
2. 双击 `run_competition_gui.bat`；现场训练过模型时点击“模型管理”应用新模型。
3. 点击“通信/高级”，只填写 DVS 通信页：模式、端点、触发串、结束符、分隔符。
4. 点击“任务向导”，选择最接近题型，按任务书填写字段、阈值、路线和示教位姿。
5. 若题目有固定取料/送检步骤，点击“动作编辑器”填写示教位姿和动作顺序。
6. 点击“赛前自检”，先选“否”做离线检查；接线后再选“是”做只连接、不运动的硬件检查。
7. 运行单帧 YOLO 或“DVS 触发/取结果”，机器人动作保持关闭。
8. 核对类别、原始字段、判定、路线、抓取位姿和放置位姿。
9. 空场、低速、单件运行；确认后再使用连续任务。

## 三、题型到向导的映射

| 任务书关键词 | 向导题型 | 必填内容 | 生成的核心能力 |
|---|---|---|---|
| 通信、触发、读取坐标 | 仅通信/读取结果 | 返回字段顺序 | 触发、解析、标准化 |
| 合格/不合格、缺陷检测 | 缺陷或状态 OK/NG 分拣 | 状态字段、OK/NG 值、路线 | 状态判定、条件路由 |
| 颜色、类别、型号、形状 | 颜色/类别/型号分拣 | 类别字段、值到路线映射 | 多类别分流 |
| 长宽直径、上下限、公差 | 尺寸测量与公差判定 | 测量字段、min/max | 自动 OK/NG 与越界原因 |
| 测量后放入合格/废料区 | 尺寸公差判定后分拣 | 公差、路线、放置点 | 测量、路由、抓放 |
| 字符、序列号、一维码、二维码 | OCR/条码/二维码识别 | 本题要求的识别字段 | 非空校验与结果展示 |
| 编码必须正确、白名单、格式校验 | OCR/条码/二维码核验后分拣 | equals/allowed/regex 规则 | 自动 OK/NG 与分流 |
| 同时要求缺陷、尺寸、编码、分流 | 综合自定义任务 | 勾选多个结果处理能力 | 多条件联合判定 |
| 孔位、装配位、定位引导 | 视觉定位引导抓取/装配 | 抓取 XYZ、放置 XYZ | 动态抓取与动态装配 |
| 取料到检测区、检测后分流 | 固定取料送检后按结果分流 | 动作序列、状态、路线 | 固定送检 + DVS + 分流 |
| 多槽位、依次摆放 | 任意分拣题型 + 顺序槽位 | 同一路线重复填写多行位姿 | 槽位计数与满位停止 |
| 码垛、分层堆叠 | 任意分拣题型 + 自动堆叠 | 基准位姿、层高、层数 | 自动计算每层 Z |

## 四、DVS 报文填写

| DVS 返回 | 分隔符 | 返回字段顺序 |
|---|---|---|
| `NG,312.5,-18.2,100` | `,` | `status,x,y,z` |
| `red 312.5 -18.2 100` | `whitespace` | `label,x,y,z` |
| `{"status":"OK","x":312.5}` | 任意 | 留空，JSON 自动解析 |

主动触发时勾选“本程序先发送”，触发串按任务书填写。DVS 主动上报时取消勾选，触发串留空。

通信角色必须成对：

| DobotVisionStudio 角色 | 本程序模式 |
|---|---|
| 串口一端 | `serial`，选择配对的另一 COM 口 |
| TCP 服务端 | `tcp_client`，填写 DVS 地址和监听端口 |
| TCP 客户端 | `tcp_server`，填写本机监听地址和端口 |

## 五、位姿与动作表

所有位姿均为基坐标系 `[X,Y,Z,Rx,Ry,Rz]`。建议每个工作点都配一个更高的接近点：

| 位姿名建议 | 含义 |
|---|---|
| `input_above` / `input_pick` | 取料区接近点 / 抓取点 |
| `inspection_above` / `inspection_place` | 检测区接近点 / 放置点 |
| `inspection_pick` | 检测完成后的取回点 |
| `output_above` / `output_place` | 出料区接近点 / 放置点 |

动作编辑器支持：`MovJ`、`MovL`、低速 `MovL`、吸盘开关、等待、返回安全点。首次执行前必须单独运行“预览并校验固定动作序列”。

## 六、三道安全门

1. 离线门：赛前自检无“阻止”项。
2. 数据门：DVS 原始字段、判定、路线和计划位姿完全正确。
3. 动作门：空场、低速、单件验证完成后，才允许连续运行。

任何一门未通过，都不得启动机器人动作。

## 七、现场故障速查

| 现象 | 处理 |
|---|---|
| TCP 超时 | 核对客户端/服务端角色、防火墙、IP、端口和网段 |
| 串口打不开 | 核对 COM 配对、波特率、是否被 DVS 或其他程序占用 |
| 一直等不到结果 | 核对主动触发/主动上报、触发串、发送与接收结束符 |
| 字段数不匹配 | 核对分隔符和返回字段顺序；JSON 时字段顺序留空 |
| 缺少必需字段 | 在向导字段别名表中加入 DVS 实际字段名 |
| 无对应路线 | 在路线表补齐实际返回值，注意大小写和空格 |
| 缺少路线位姿 | 每条路线至少填写一个放置点 |
| 槽位或堆叠已满 | 清空实体工件后运行“清零槽位/堆叠计数” |
| Z 低于安全下限 | 查坐标系、单位、字段顺序、标定和补偿，不得降低安全线 |
| 急停后无法继续 | 清障并人工确认，再重新连接和进行一次无运动读取 |

## 八、赛前离线备份

1. 在联网电脑双击 `prepare_offline_dependencies.bat` 下载 Windows 离线安装包。
2. 双击 `prepare_field_package.bat` 运行自检并生成完整 ZIP 备份。
3. 把工程目录和 ZIP 分别复制到两个 U 盘。
4. 备用电脑先双击 `install_offline.bat`，再运行 `run_competition_gui.bat`。
5. 打印 `FIELD_QUICK_CARD.html`，现场不依赖电子文档搜索。

连续运行后点击“导出结果”，程序会把逐件字段、识别值、OK/NG、路线和分类计数保存到 `runs/results` 的 JSON/CSV 中。

正式任务书未知，因此不能承诺覆盖尚未公开的新硬件或私有协议；当前方案把已知机器视觉赛题拆成通信、字段、判定、路由、位姿和动作六层，常见组合题无需再修改代码。
