# 今日赛题执行清单

正式题面已整理在 `../gs/2026-08-18_今日国赛题整理.md`。本清单只记录软件必须完成的可验收行为。

## 必须保分的顺序

1. 模块 D 的 GUI 结构、三个按钮、RGB/Depth/YOLO 推理图、监控字段与 3D 结果表。
2. 模块 E 的“拍照 -> 检测 -> 坐标解算 -> 高度优先抓取 -> 指定组装位”闭环，先在模拟模式验证。
3. 模块 C 的 7 类数据集、训练、模型热加载和置信度门槛；模型不能由通用水果权重替代。
4. 模块 B 的 DVS 输出适配层与文本结果文件落盘；在现场将实际 DVS 流程映射到规定字段。
5. A 的安装、布线和示教只能现场完成，程序只提供检查与记录，不能替代实物验收。

## 不能在当前照片中确定、必须现场填写的参数

- 7 个组装位的真实 6D 位姿、拍照点、最高安全点和抓取 Z 偏置。
- 3D 相机内参及重做后的手眼矩阵。
- DVS 工程的通信方式、触发命令、返回字段和尺寸/缺陷实际输出格式。
- 题面图示的精确布局尺寸、颜色与最终标签顺序。

## 对明天的准备原则

不要把今日的类别、输出字段、组装位写死在业务代码。把它们放在赛题配置中；明天只改类名、槽位位姿、DVS 字段映射、测量项目和判定阈值，GUI 和动作链不重写。

详细概率与明早操作顺序见 `TOMORROW_TASK_PREDICTION.md`。
