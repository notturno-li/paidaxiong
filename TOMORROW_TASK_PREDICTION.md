# 2026-08-19 赛题预测与应变表

> 预测依据是 2026-08-18 正式题面（页 2--16）的模块结构。以下不是官方信息；只预测“变化层”，不猜具体数值。

## 结论

明天最可能保留 A--E 五模块、150 分钟、15 分钟演示和 Python 自建 GUI 这一骨架；一定会变化的高概率部分，是模块 B 的具体物料/输出字段，以及模块 E 的 5 件物料组合、摆放姿态和组装位要求。

| 明日可能变化 | 概率 | 今日证据 | 准备方式 |
| --- | ---: | --- | --- |
| 随机 5 件物料的类别组合、位置和旋转角变化 | 极高 | 今日明确由裁判随机放置，7 类中最终抓 5 件 | 不写固定抓取顺序；继续按高度优先、先外后内动态排序 |
| 组装单元的目标格位/目标角度变化 | 很高 | 今日按图 19 摆放，属于最容易替换的题面图 | 全部目标位放 YAML；现场只重新示教 `robot.bins` |
| OCR/二维码的具体内容和 OK/NG 比对值变化 | 极高 | 今日要求识别并对比题面给定值 | 比对值、分隔符、字段名放配置，不写在 Python 判断里 |
| 尺寸测量对象或“直径/距离/夹角”组合变化 | 很高 | 今日图 12 明确列出 5 个量和两类误差门限 | DVS 输出整行原始字段；GUI 监控项从配置生成 |
| 缺陷形态、缺陷数量和面积变化 | 很高 | 今日只限定输出结构，不固定缺陷位置 | 保留二值化/形态学/连通域参数预设，现场调阈值 |
| 齿轮齿数、直径或正常/缺齿样件变化 | 很高 | 今日公式固定但样件由题面提供 | 齿数和直径实时测量，不能录入固定答案 |
| 模块 B 将某一题替换成同类视觉题（颜色、轮廓、孔位、字符） | 中 | B 占 27 分且包含多个可替换算子任务 | 预留原始文本显示和可配置监控字段，先保通信和 GUI 分 |
| 7 类标签集合整体变化 | 中低 | 今日模块 C 明确给出 7 个标签，竞赛硬件和组装板通常固定 | 数据集工具允许新建项目；不要覆写今日模型 |
| A/C/D/E 评分结构或按钮名称大改 | 低 | 这些内容与统一硬件和验收界面强绑定 | 今日专用 GUI 保留；另复制配置应对小改，不重写主流程 |

## 明早拿到题后的 10 分钟动作

1. 先对照评分表确认模块分值、三个按钮名称、监控字段和自动流程是否变化。
2. 复制 `configs/today.yaml` 为当天配置，只改类名、模型路径、5 件数量、拍照点和 7 个组装位。
3. 将 DVS 四个输出文件各跑一次，确认文本编码、分隔符和字段顺序；GUI 始终保留原始行，避免解析失败丢分。
4. 空载运行拍照点和所有组装位，再以单件低速验证 Z，最后才运行 5 件自动流程。
5. 新模型必须使用新目录，先看每类样本数、混淆矩阵和实拍置信度；不能用今日模型覆盖明日数据。

## 当前程序对应能力

- `configs/today.yaml`：7 类、5 件、拍照点、组装位和监控字段均为配置项。
- `app/today_main.py`：固定保留 RGB、Depth、YOLO 三画面与题面规定的三个按钮。
- `task_outputs.monitor_fields`：明日字段名变化时可改显示项，无需重新布局 GUI。
- 自动抓取按检测高度优先，避免把今日的固定顺序带到明日随机摆放。
